var assert = require('assert');
var depict = require('../');

console.log(depict);

describe('utility', function() {
  describe('#cleanURL()', function() {
    it('should prepent `http://` if no protocol is specified', function() {
      assert.equal(0, 0);
    });
  });
});

